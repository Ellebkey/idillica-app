# En dev/preview no hace falta: Vite hace proxy de /api al backend (ver vite.config.ts).
# Define VITE_API_URL solo para apuntar a un backend remoto; ese origen debe estar
# permitido por el CORS del backend (FRONTEND_URL).
# VITE_API_URL=https://api.idilica.app/api
